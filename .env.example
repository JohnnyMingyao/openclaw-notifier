# Copy to .env (or export in shell) before running watcher
# REQUIRED: operator token for Gateway WebSocket auth
OPENCLAW_GATEWAY_TOKEN=__PASTE_YOUR_GATEWAY_REMOTE_TOKEN_HERE__

# Optional: override websocket URL
# GATEWAY_URL=ws://127.0.0.1:18789
